
using UnityEngine;

public class SnailFightScript : MonoBehaviour
{
    public Animator _snailAnimator;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
